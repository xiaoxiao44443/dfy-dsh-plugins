# 换设备继续开发：DSH 0.1.7-alpha.1 与组合包

用户要求暂停后续验证，提交并推送当前进度；本次不发布 npm 或桌面 Release。

## 当前改动

- 8 个维护中插件和公共库适配 DSH 0.1.7-alpha.1；视觉理解停更并退出工作区构建和发布。
- 旧配置迁移、原生附件、外观逐段折叠及壁纸侧栏适配详见 compatibility-0.1.7-alpha.1.md。
- 新增 `plugins/bundle`，包名 `@dfy-plugins/dsh-bundle`，版本 0.1.0。只有装配清单，8 个依赖采用 `workspace:*`，发布时转成精确版本。
- 组合清单沿用原插件 ID；官方详情页支持总开关及各条目开关。桌面端识别 `dfy.includes`，避免把包内组件误判为未安装。
- 发布准备和安装测试脚本已纳入组合包；安装测试分独立插件和组合包两个临时 Profile，尚未完整执行。

## 已进行和未完成

- 本轮开始前的 0.1.7 适配验证见兼容记录。
- 新增组合清单测试通过；目录校验通过；工作区依赖已安装并更新锁文件。
- npm 归档准备在用户要求暂停后中止；不要复用此次 `release/npm` 目录发布。
- **组合包的实际安装、卸载、热启停、界面验收和旧安装迁移尚未完成。**
- 用户正在使用的 web Profile 仍为原来的单独本地链接，未迁移组合包。
- 尚未发布组合包到 npm。线上 `catalog.json` 暂不加入未发布包；新入口在 `catalog.preview.json`，发布并验证后再移入线上目录。

## 下一步

1. 拉取此仓库和 dfy-dsh-desktop，保持 Electron 43.4.0。
2. 安装依赖并构建插件；先在临时 DSH_HOME 中验证组合包，确认每个条目只加载一次、配置和停用状态保留。
3. 验证 `release:test-install` 的两种安装形式及依赖解析，处理发现的问题；需要时重新运行 `release:prepare`。
4. 开发版使用 `DFY_PLUGIN_CATALOG_FILE=/绝对路径/dfy-dsh-plugins/catalog.preview.json pnpm dev` 预览目录。
5. 在迁移用户 Profile 前停止对应 Harness，移除单独安装记录并保留配置与数据；当前桌面端会阻止混装，但尚未提供自动迁移按钮。
6. 完成验证后按用户后续要求决定发布；不要把本次提交视为已通过整体验收的 Release。
